replicaCount: 1

image:
  repository: 7imbrook/postgrest

healthEndpoint: "/"

container:
  port: 3000

ingress:
  enabled: true
  hosts:
    - timbrook.tech
    - timbrook.dev
    - timbrook.life
  paths:
   - /api/p/

vault:
  enabled: true
  host: "http://vault.core.svc.cluster.local"
  role: "api"

resources:
  limits:
   cpu: 200m
   memory: 256Mi
  requests:
   cpu: 100m
   memory: 128Mi
